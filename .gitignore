# Node
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files
.env

# PDFs and uploaded files (we won't push them to GitHub)
pdfs/**/*.pdf

# Local JSON data – keep only on server, not on GitHub
data.json
users.json

# VS Code
.vscode
